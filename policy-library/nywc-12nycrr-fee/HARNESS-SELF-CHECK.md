# Harness self-check: the comparator shown failing

Generated by `harness-selfcheck.mjs` on the committed `determinations.json` (600 determinations). Do not edit by hand: rerun it.

An instrument that cannot fail is not evidence. Before any divergence figure from `compare.mjs` is read, one mutation is injected into the in-force register in memory and the comparator must catch it; then the unmutated register is replayed twice and the comparator must find nothing.

## 1. ONE CONSTANT OF THE IN-FORCE REGISTER

**What was perturbed.** `12nycrr/329-1.3/c/3/eighty-five-percent`, the argument `85` of `amount_fraction_of`, set to `80`. Nothing else. The mutated register exists only in memory; the file on disk is untouched.

```
COMPARE  in-force (unmutated)  vs  in-force MUTATED: 85 -> 80 at 12nycrr/329-1.3/c/3/eighty-five-percent
denominator: 600 determinations; 74 clauses in the union of both runs
clauses on which at least one determination diverges: 1 of 74
determinations diverging on at least one clause: 33 of 600 (on a result token: 33 of 600; the rest only on clause absence)
  12nycrr/329-1.3/c/3/eighty-five-percent              diverge 33/600; agree 567/600
        33  equal -> not_equal
RESULT: DIVERGENT. [exit 1]
```

Determinations changed on the mutated clause: **33 of 600**. Determinations changed on any OTHER clause: **none**.

Comparator exit status on the mutated comparison: **1 (nonzero, as required)**.

## 2. THE UNMUTATED REGISTER, TWICE

```
COMPARE  in-force (unmutated)  vs  in-force (unmutated, second run)
denominator: 600 determinations; 74 clauses in the union of both runs
clauses on which at least one determination diverges: 0 of 74
determinations diverging on at least one clause: 0 of 600 (on a result token: 0 of 600; the rest only on clause absence)
RESULT: IDENTICAL on every clause of every determination. [exit 0]
```

Comparator exit status on the unmutated comparison: **0 (as required)**.

## Verdict

**SHOWN FAILING, THEN CLEAN.** The comparator detects a single-constant mutation on the clause that carries it (33/600 determinations) and exits nonzero, and reports zero divergence on an identical re-run. The divergence figures in DIVERGENCE.md may be read.
