# Harness self-check: citation mutant shown failing

Generated by `check.mjs` on the committed determinations (8 determinations). Do not edit by hand.

An instrument that cannot fail is not evidence. Before any divergence figure is read, the Wise register is encoded as citing the PSR APP requirement (the citation clause emits `present`) and the assertion fails; then the unmutated register is replayed and the WIRED absence is green.

## 1. ENCODED AS CITING PSR APP (shown failing)

**What was perturbed.** `wise/2978048/citation/psr-app` evaluate set to `const present`. The file on disk is untouched.

Determinations changed on the mutated clause: **8/8 (100.0%) [population: 8 synthetic determinations, seed enumerated-not-random, parameters sha256 48f824ec585f from generate-determinations.mjs header; REPOSITORY-INTERNAL: does not leave op-policy-engine in any form until an equivalent is computed over a real history]**.

Citation-mutant gate: **failed, as required**.

## 2. WIRED ABSENCE (green)

Clean interpret: `wise/2978048/citation/psr-app` result `absent` on empty citation fact. Clean rerun divergence: **0/8 (0.0%)**.

[population: 8 synthetic determinations, seed enumerated-not-random, parameters sha256 48f824ec585f from generate-determinations.mjs header; REPOSITORY-INTERNAL: does not leave op-policy-engine in any form until an equivalent is computed over a real history]

**SHOWN FAILING, THEN CLEAN.** The completeness gate may be read.
